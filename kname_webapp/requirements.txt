# 배포 의존성.
#
# 상한(<N)만 걸고 하한은 걸지 않는다.
#   - 하한이 없으므로 지금 설치되는 버전은 예전과 똑같다(늘 최신을 받는다).
#   - 상한이 있으므로 **다음 메이저 버전이 나와도 조용히 올라타지 않는다.**
#
# 상한이 없으면 코드를 한 줄도 안 바꿨는데 재배포하는 날 갑자기 깨진다.
# 무엇이 바뀌었는지 알 수 없어 원인 찾기가 가장 어려운 종류의 고장이다.
# 실제로 anthropic 은 이미 1.x, pandas 는 3.x 가 나와 있다.
#
# 더 확실하게 하려면 서버에서 돌고 있는 정확한 버전으로 박는 것이 낫다.
# Render Shell 에서 아래를 실행해 나온 값을 == 로 고정하면 된다.
#   pip freeze | grep -iE "flask|pandas|openpyxl|anthropic|gunicorn|sentry|texttospeech"
flask<4
pandas<4
openpyxl<4
anthropic<2
gunicorn<27
sentry-sdk[flask]<3
google-cloud-texttospeech<3
